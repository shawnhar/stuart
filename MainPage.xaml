<Page
    x:Class="Stuart.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stuart"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!--The editing UI-->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="auto">

            <StackPanel Orientation="Vertical">
                <ListView x:Name="editList"
                          CanDragItems="true"
                          AllowDrop="true"
                          CanReorderItems="true"
                          DragItemsStarting="EditList_DragItemsStarting">
                    
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <local:EditGroupControl/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel Orientation="Horizontal">
                    <Button Content="+" Click="NewEdit_Click"/>
                    <Button Content="Trash" AllowDrop="True" DragEnter="TrashCan_DragEnter" Drop="TrashCan_Drop"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!--The photo itself -->
        <ScrollViewer x:Name="scrollView"
                      Grid.Column="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto"
                      ZoomMode="Enabled">
            
            <canvas:CanvasControl x:Name="canvas"
                                  CreateResources="Canvas_CreateResources"
                                  Draw="Canvas_Draw"/>
        </ScrollViewer>
    </Grid>
</Page>
