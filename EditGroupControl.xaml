<UserControl
    x:Class="Stuart.EditGroupControl"
    x:Name="control"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stuart"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="200">

    <StackPanel Orientation="Vertical" MinWidth="180">
        <StackPanel Orientation="Horizontal">
            <ToggleButton Content="on" IsChecked="{Binding IsEnabled, Mode=TwoWay}"/>
        </StackPanel>

        <ListView x:Name="effectList"
                  ItemsSource="{Binding Effects}"
                  SelectedValue="{Binding Parent.SelectedEffect, Mode=TwoWay}"
                  CanDragItems="true"
                  AllowDrop="true"
                  CanReorderItems="true">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <ComboBox ItemsSource="{Binding ElementName=control, Path=EffectTypes}"
                                  SelectedItem="{Binding Type, Mode=TwoWay}"/>
                        <TextBlock Text="     . . ."/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Content="+" HorizontalAlignment="Right" Click="NewEffect_Click"/>
    </StackPanel>
</UserControl>
