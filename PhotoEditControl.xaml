<UserControl
    x:Class="Stuart.PhotoEditControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stuart"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="200">

    <StackPanel Orientation="Vertical" MinWidth="100">
        <StackPanel Orientation="Horizontal">
            <ToggleButton Content="on" IsChecked="{Binding IsEnabled, Mode=TwoWay}"/>
        </StackPanel>

        <ListView x:Name="effectList"
                  CanDragItems="true"
                  AllowDrop="true"
                  CanReorderItems="true"
                  DragItemsStarting="EffectList_DragItemsStarting"
                  ItemsSource="{Binding Effects}">
        </ListView>

        <Button Content="+" HorizontalAlignment="Right" Click="NewEffect_Click"/>
    </StackPanel>
</UserControl>
